services:
  wkmigrate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wkmigrate
    env_file:
      - .env
    volumes:
      # Mount a local directory for pipeline output
      - ./output:/app/output
