resources:
  jobs:
    test_adf_pipeline:
      name: test_adf_pipeline
      schedule:
        quartz_cron_expression: 0 5,30 1,20 ? * 1,3
        timezone_id: UTC
        pause_status: PAUSED
      tasks:
        - task_key: run_test_notebook_2
          depends_on:
            - task_key: for_each_task
          notebook_task:
            notebook_path: /Users/gregory.a.hansen@gmail.com/test_notebook_2
            source: WORKSPACE
          max_retries: 1
          min_retry_interval_millis: 0
          timeout_seconds: 43200
          description: Run test_notebook_2 in ADB
        - task_key: for_each_task
          for_each_task:
            inputs: '["a", "b", "c"]'
            concurrency: 2
            task:
              task_key: for_each_task_inner_tasks
              run_job_task:
                job_id: 539746167836958
      tags:
        CREATED_BY_WKMIGRATE: ""
